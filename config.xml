<?xml version="1.0" encoding="UTF-8" ?>
    <widget xmlns   = "http://www.w3.org/ns/widgets"
        xmlns:gap   = "http://phonegap.com/ns/1.0"
        id          = "com.batc.socceranalytics"
        versionCode = "1" 
        version     = "1.0.0" >

    <name>Soccer Analytics</name>

    <description>
        A soccer statistics tracking application.  
    </description>

    <author href="http://batc.i2r.a-star.edu.sg/" email="stuopy@batc.i2r.a-star.edu.sg">
        BATC Intern Team
    </author>

    <content src="app.html" />
    <access origin="*" />

    <gap:platform name="ios" />  	<!-- build on ios only  -->
    <icon src="icon.png" />
    
    <!-- Customize your app and platform with the preference element. -->
    <preference name="phonegap-version"           value="3.5.0" />          <!-- all: current version of PhoneGap -->
    <preference name="orientation"                value="landscape" />      <!-- all: lock in landscape mode -->
    <preference name="target-device"              value="tablet" />         <!-- targets tablet devices -->
    <preference name="fullscreen"                 value="true" />           <!-- all: hides the status bar at the top of the screen -->
    <preference name="DisallowOverscroll"  		  value="true"/> 			<!-- Do not show feedback for scrolling past beginning or end of content-->
    <preference name="stay-in-webview"            value="true" />          <!-- ios: external links should open in the default browser, 'true' would use the webview the app lives in -->
    <preference name="ios-statusbarstyle"         value="black-opaque" />   <!-- ios: black-translucent will appear black because the PhoneGap webview doesn't go beneath the status bar -->
    <preference name="HideKeyboardFormAccessoryBar" value="true"/> 			<!-- ios/blackberry: prevent additional toolbar that appears above keyboard which helps to navigate user from one form to another--> 
    <preference name="exit-on-suspend"            value="false" />          <!-- ios: if set to true, app will terminate when home button is pressed -->
    <preference name="show-splash-screen-spinner" value="true" />           <!-- ios: if set to false, the spinner won't appear on the splash screen during app loading -->
    <preference name="auto-hide-splash-screen"    value="true" />           <!-- ios: if set to false, the splash screen must be hidden using a JavaScript API -->
    <preference name="disable-cursor"             value="false" />          <!-- blackberry: prevents a mouse-icon/cursor from being displayed on the app -->
    <preference name="android-minSdkVersion"      value="7" />              <!-- android: MIN SDK version supported on the target device. MAX version is blank by default. -->
    <preference name="android-installLocation"    value="auto" />           <!-- android: app install location. 'auto' will choose. 'internalOnly' is device memory. 'preferExternal' is SDCard. -->
</widget>